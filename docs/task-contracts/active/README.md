# Active Task Contracts

Use this directory for branch-local active task contracts.

Rules:

- keep one focused contract per active task slice
- replace provisional finish checks with task-specific checks before treating the task as done
- move completed contracts to `docs/task-contracts/completed/`
